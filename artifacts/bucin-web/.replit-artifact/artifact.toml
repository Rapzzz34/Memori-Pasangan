kind = "web"
previewPath = "/"
title = "Kenangan Kita"
version = "1.0.0"
id = "artifacts/bucin-web"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24908

[services.development]
run = "pnpm --filter @workspace/bucin-web run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/bucin-web", "run", "build" ]
publicDir = "artifacts/bucin-web/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24908"
BASE_PATH = "/"
